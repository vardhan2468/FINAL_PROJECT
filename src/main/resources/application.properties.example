# Environment-specific configuration
# Copy this file to application-local.properties and set your local values
# application-local.properties is ignored by git (.gitignore)

# Database Configuration
DB_USERNAME=root
DB_PASSWORD=your_password_here

# Usage: Set these as environment variables or use application-local.properties
# For development, you can run with: mvnw spring-boot:run -Dspring-boot.run.arguments="--DB_PASSWORD=yourpassword"
